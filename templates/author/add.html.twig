<h1>Add Author</h1>

{{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.save) }}
{{ form_end(form) }}

<a href="{{ path('app_show_author') }}">Back to list</a>
